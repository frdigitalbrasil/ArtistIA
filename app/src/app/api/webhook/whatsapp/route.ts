import { NextResponse } from "next/server";

// POST /api/webhook/whatsapp — webhook Evolution API (Fase 3)
export async function POST() {
  return NextResponse.json({ message: "Not implemented" }, { status: 501 });
}
