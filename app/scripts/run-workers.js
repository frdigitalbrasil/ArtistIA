/**
 * Entry point para os workers em background (BullMQ).
 * Em produção, aqui seriam iniciados planning-worker, scraping-worker, etc.
 */
// TODO: importar e iniciar os workers quando implementados
// require('../dist/workers/planning-worker');
console.log('ArtistAI workers — aguardando jobs...');
setInterval(() => {}, 60000);
